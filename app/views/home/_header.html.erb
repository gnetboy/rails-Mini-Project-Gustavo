<nav class="navbar navbar-dark bg-dark">
  <div class="container-fluid">
         <a class="navbar-brand">Rails App</a>
           <% if user_signed_in? %>
         
             <div class="nav-item">
               <%=link_to "Students", 
              user_students_path(current_user.id), 
               class:"nav-link"%>
             </div> 
              <div class="nav-item">
               <%=link_to "Destinations", 
               user_countries_path(current_user.id), 
               class:"nav-link"%>
             </div> 

          <div class="nav-item">
             <%=link_to "Edit Profile", 
              edit_user_registration_path, 
              class:"nav-link"%>
           </div> 
           
           <div class="nav-item">
             <%=link_to "Sign Out", 
             destroy_user_session_path,
             method: :delete, 
             class:"nav-link"%>
           </div>
      <div class="d-flex">
         <%=form_with(url: search_path, method: 'get',local:true ) do %>
            <div class="form-group">
              <%= text_field_tag :country, nil, placeholder: "search for a country", class: 'form_control' %>
              <%= button_tag 'Search', class:"btn btn-outline-success" %>
         <% end %>
            <div>
        </div>
           <% else  %>
           <div class="nav-item">
           <%=link_to "Sign Up", 
           new_user_registration_path,
             
           class:"nav-link"%>
           </div>
           
            <div class="nav-item">
           <%=link_to "Sign In", 
           new_user_session_path,
           class:"nav-link"%>
           </div>
           <% end %>
        </div>
  
</nav>